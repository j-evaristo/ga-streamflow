# Downloaded + built data — regenerated by download_ga_discharge.py / build_viewer_data.py.
# The GitHub Actions workflow rebuilds all of it on every run, so none of it is committed.
data/

__pycache__/
site/

